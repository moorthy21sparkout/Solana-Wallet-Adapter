import { WalletFormatPipe } from './wallet-format.pipe';

describe('WalletFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new WalletFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
